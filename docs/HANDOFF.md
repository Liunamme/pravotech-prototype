# HANDOFF — контекст для продолжения (перезапуск / компакт / смена модели)

Прочитай этот файл ПЕРВЫМ при возобновлении. Он даёт актуальное состояние;
глубокие контракты — в остальных файлах `docs/` и в `DEVLOG.md`.

Рабочая директория: `/Users/liunamme/Desktop/PravoTexTZ`
Dev-сервер: `npm run dev -- --port 5199` → http://localhost:5199 (роутинг hash, напр. `/#/today`).

---

## 1. Что это за проект

Прототип «ПравоТех» — B2B-платформа, где AI-агент выполняет работу юриста; юрист
подтверждает / изменяет / отклоняет предложения агента. React + TypeScript, Vite,
CSS Modules, Radix UI, Zustand. Агент замокан (сценарии в `src/core/agent`).

Базовый прототип (этапы 1–9) и редизайн десктопа по макету — **готовы**.
Подробности — `DEVLOG.md`, `docs/SPEC.md`, `docs/DOMAIN.md`, `docs/SCENARIOS.md`,
`docs/UX.md`, `docs/APP-STRUCTURE.md`. Макет — `design/design_handoff_pravotex_ui/`.

## 2. Где мы сейчас

Прототип **опубликован**: https://liunamme.github.io/pravotech-prototype/

Пройдено: десктоп → предрелизное укрепление по внешнему аудиту
(`docs/CODE_REVIEW_PREDEPLOY.md`) → деплой на GitHub Pages → планшетная
раскладка → мобильная раскладка. Всё влито в `main`. Подробности решений — в
`DEVLOG.md`.

Адаптив закончен на всех трёх ширинах (пороги — `shared/lib/breakpoints.ts`):

| Класс | Ширина | Раскладка |
|---|---|---|
| Десктоп | ≥1280 | три колонки |
| Планшет | 768–1279 | рейка и сайдбар остаются, очередь — в `QueueDrawer` |
| Телефон | <768 | нижние вкладки, по одной поверхности за раз |

Плюс правило: окно ниже 500px считается мобильным при любой ширине (телефон в
альбоме, открытая клавиатура).

Проверки зелёные: `npm run lint`, `npx tsc -b`, `npm test` (77),
`npm run build`, `npm run check:dist`, `npm run dry-run`,
`npm audit --omit=dev`.

**Дальше по плану:** заказчик присылает общие баги — их чинить сразу во всех
трёх раскладках, начиная с десктопа.

## 3. Git

`main` содержит десктопную часть. Ветки `fix/*` и `feature/redesign-desktop` —
историческая цепочка, все влиты в `main`.

`chore/predeploy-hardening` — правки по аудиту, в `main` ещё **не влита**
(ждёт решения заказчика). Её подветки `chore/predeploy/*` влиты в неё
fast-forward, по одной на пункт аудита.

Под каждый новый баг заказчик просит **отдельную ветку** `fix/<что>`.

## 4. ⭐ Стиль работы заказчика (соблюдать строго)

- **По ОДНОЙ задаче за раз.** Даёт баг → фиксишь → проверяет → следующий.
- **НЕ коммитить без явного «коммить».** Сделал → показал скриншот/замер → ждёшь.
- **Под каждый баг — отдельная ветка** `fix/<что>`.
- **Проверять визуально и замерами.** Chrome-расширение в этой среде не работает;
  используется Playwright напрямую: скрипты `.mjs` в
  `<scratchpad>/pw/`, запуск `cd <scratchpad>/pw && node <script>.mjs`, порт 5199.
  Скриншоты читать через Read, важные — отдавать заказчику.
- Заказчик ценит **числа**: замеры отступов, computed-цвета, покадровые замеры
  анимации. Если баг не воспроизводится — честно сказать это и показать, что
  именно мерил.
- Отвечать по-русски, по делу.

## 5. ⭐⭐ Технические уроки (грабли, на которые уже наступали)

1. **`backdrop-filter` ломает прокрутку.** Элемент с фильтром становится
   «backdrop root», композитор Chromium перестаёт считать его целью колеса и
   отдаёт событие тому, что ПОД ним. Лечение: фильтр в отдельный инертный слой
   (`pointer-events: none`), содержимое — выше него без фильтра. Сделано в
   `DocumentInspector` (`.glass`). Если где-то ещё «проваливается» ввод — та же
   болезнь.
2. **`mask-image` на предке отключает `backdrop-filter` внутри.** Предок с
   маской — тоже backdrop root, и размытие внутри перестаёт работать. Из-за
   этого в очереди края отделаны вуалями, а не маской (`CardQueue`).
3. **Родной `scrollIntoView` тащит всю раскладку.** Он подкручивает КАЖДОГО
   предка, включая `overflow: hidden` и страницу. Использовать только
   `shared/lib/scrollIntoViewWithin.ts` (двигает единственный контейнер).
4. **`Tooltip.Trigger asChild` + `NavLink className={fn}`** — Radix склеивает
   className строкой, функция попадает в атрибут исходным текстом, стили не
   применяются. Активное состояние ссылок брать из `[aria-current='page']`.
5. **Акцентный цвет на акцентной подложке сливается.** На фиолетовом фоне текст
   берёт `--pt-text` (белый в тёмной, тёмный в светлой). Тот же принцип у
   активной иконки рейки.
6. **Скрим модалки не должен брать `--color-text`** — в тёмной теме он почти
   белый. Есть отдельный токен `--pt-scrim`.
7. **Родные контролы** (иконка `input[type=date]`, календарь, выпадашки) красит
   только `color-scheme` — он задан в `tokens.css` для обеих тем.
8. **Никаких literal-цветов вне `tokens.css`**: проверка
   `grep -rnE '#[0-9a-fA-F]{3,6}' src --include='*.module.css'`.
9. Полосу под логотип резервирует токен `--logo-safe-top` (рейка, настройки,
   витрина) — иначе логотип ложится на заголовок.
10. **React 19 считает возвращённое ref-колбэком значение функцией очистки.**
    `ref={(el) => el && f(el)}` больше не проходит типизацию — тело колбэка
    должно быть в скобках и ничего не возвращать.
11. **Ядро не имеет права импортировать конкретное пространство.** Если
    `core/**` понадобилась утилита из `workspaces/**`, её место —
    `shared/lib/**` (так `dateInput` стал `shared/lib/isoDate.ts`).
12. **Решение по карточке — машина состояний** (`store/cardsSlice.ts`).
    Новые переходы добавлять в `ALLOWED_TRANSITIONS`, иначе действие молча
    погасится с предупреждением в dev-консоли.
13. **Вендорный префикс пишется ПЕРЕД стандартным свойством.** Минификатор
    CSS (lightningcss в Vite) считает `-webkit-foo` алиасом `foo` и
    схлопывает пару, оставляя последнее объявление. Из-за обратного порядка
    из сборки выживал только `-webkit-backdrop-filter`, и всё матовое стекло
    переставало размываться везде, кроме Safari. В dev не воспроизводится:
    там CSS не минифицируется. Сторож — `npm run check:dist`, гоняется в CI
    после сборки.

## 6. Эталонные решения (переиспользовать)

- **Динамическое затухание краёв прокрутки** — `shared/lib/useScrollFade.ts`:
  `--fade-top/-bottom` включаются, только когда за краем есть скрытый контент.
  Применено в списке договоров/дел.
- **Размытые вуали у краёв ленты** — `CardQueue`: sticky-полосы сверху/снизу с
  `backdrop-filter` и маской, включаются динамически. Sticky-заголовки групп
  фона не имеют.
- **Единая подсказка** — `shared/ui/Tooltip` (стекло оверлея). Своих копий
  заводить нельзя, рейка ходит через неё же.
- **Ссылки на документы** везде открывают инспектор и пишут `?doc=&anchor=`:
  чипы, строки сроков, предмет карточки.

## 7. Открытые хвосты

- Собрать Docker-образ: в среде разработки Docker недоступен, проверена только
  сборка из урезанного контекста.
- Шапка инспектора: 7 строк меты вместо 3 по макету (`Номер`, `Вид`, `Дата`,
  `Арендодатель` дублируют заголовок и «Стороны»). Предлагал почистить —
  заказчик решение не принял.
- Мобильные вкладки контекста без счётчиков: в макете «Договоры 4», у нас без
  числа. `ContextTabDef` количества не отдаёт — нужно расширять контракт
  манифеста. Заказчику показывал, решение не принято.
- Восстановление последнего диалога при переходе между пространствами включено
  только на телефоне. На десктопе список диалогов виден всегда, и приглашение
  там осмысленная стартовая точка. Заказчику предложено — решение не принято.
- Заголовок нового треда остался «Новый разговор», хотя кнопка на телефоне
  называется «Новый диалог»: строка общая с десктопом, менять без спроса не
  стали.

## 8. Как проверить, что всё живо

```bash
npm run lint                               # oxlint, предупреждений быть не должно
npx tsc -b                                 # типы (приложение + тесты + конфиги)
npm test                                   # vitest run
npm run dry-run                            # прогон сценариев мок-агента
npm run build                              # сборка
npm audit --omit=dev --audit-level=high    # зависимости, уезжающие в браузер
npm run dev -- --port 5199                 # дев
```
